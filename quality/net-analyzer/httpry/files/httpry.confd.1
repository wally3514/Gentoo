# Config file for /etc/init.d/httpry

# Specify output format string. This is the default string.
# See the format-string file in the docs directory for more options.
FORMAT="timestamp,source-ip,dest-ip,direction,method,host,request-uri,http-version,status-code,reason-phrase"

# Interface to listen for web traffic on
IFACE="eth2"

# Specify the path and file name to log to
LOG_PATH="/var/log/httpry/httpry.log"

# Specify the user for the daemon to run as
USER="httpry"

HTTPRY_OPTS="-f ${FORMAT} -i ${IFACE} -o ${LOG_PATH} -u ${USER}"
